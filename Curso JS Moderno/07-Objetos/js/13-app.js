const producto = {
    nombre: "Monitor 20 pulgadas",
    precio: 30,
    disponible: true,
}

console.log(Object.keys(producto)) // keys
console.log(Object.values(producto)) // values

console.log(Object.entries(producto)) // keys y values