const producto = 'Monitor 20 Pulgadas';

console.log(producto.toUpperCase()); //pasa todo a mayuscula
console.log(producto.toLowerCase()); //pasa todo a minuscula

const precio = 300

console.log(precio.toString()) //lo pasas a string